# Cline Skills — AWhittleWandering

## Domain skills
- Cloudflare Workers (Wrangler deploy, D1 migrations, KV)
- Hono.js + TypeScript API structure
- Zod schema validation
- CI/CD with GitHub Actions

## Repository-specific skills
- Use `/api/v1/unified-data` as the sole client data source.
- Enforce privacy tiers and avoid raw GPS in public responses.
- Maintain provider-agnostic ingestion patterns.
- Keep migrations idempotent; verify via scripts/ci-migrations-check.sh.
