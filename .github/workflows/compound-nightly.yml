name: compound-nightly

on:
  # Schedule disabled — ci-preflight.yml runs the same security scan + quality gate on every push,
  # and Dependabot handles dependency vulnerability alerts. Nightly adds no value.
  # schedule:
  #   - cron: '30 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: compound-nightly
  cancel-in-progress: true

jobs:
  compound:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d632683dd7b4114ad314bca15554477dd762a938 # v4.2.0
      - name: Install ripgrep
        run: sudo apt-get update && sudo apt-get install -y ripgrep
      - name: Run compound loop
        env:
          COMPOUND_MAX_ATTEMPTS: 3
        run: bash scripts/compound-nightly.sh
