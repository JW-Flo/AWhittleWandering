{
  "name": "continentalusa",
  "version": "1.0.0",
  "description": "Continental USA Travel Project",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "verify-dns": "node scripts/verify-dns-change.js",
    "configure-cloudflare": "node scripts/configure-cloudflare.js",
    "setup-dns": "npm run configure-cloudflare && sleep 10 && npm run verify-dns",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
