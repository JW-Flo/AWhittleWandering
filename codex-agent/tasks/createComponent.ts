export async function createComponent({ name, props }: { name: string; props: string[] }) {
  console.log(`[Codex] Creating component ${name} with props:`, props);
  // TODO: Generate file dynamically
}
