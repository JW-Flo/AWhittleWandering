# Tessie Adapter Contract Fixtures

- `raw/` contains provider payloads captured from Tessie (sanitized).
- `canonical/` contains expected canonical events for replay tests.

Update both when adapter mappings change.