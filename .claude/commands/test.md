# Test

Run the full test suite for AWhittleWandering.

## Steps
1. Run backend tests: `cd backend/edge-worker && npm run test:run`
2. Run contract tests: `cd backend/edge-worker && npm run test:contract`
3. Run frontend type check: `cd frontend && npx tsc --noEmit`
4. Report results with pass/fail counts
